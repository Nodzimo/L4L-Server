echo
echo [L4L] Executing: l4l/dev.cfg

hostname "L4L Dev" // ""
sn_hostname_format1 "{hostname} Dev {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} Dev {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} Dev" // "[{hostname} #{servernum}]"

echo [L4L] Executed: l4l/dev.cfg
echo

exec l4l/dev_cvars
exec server