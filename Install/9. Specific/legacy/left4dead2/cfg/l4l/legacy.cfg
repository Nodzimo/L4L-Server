echo
echo [L4L] Executing: l4l/legacy.cfg

hostname "L4L Legacy" // ""
sn_hostname_format1 "{hostname} Legacy {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} Legacy {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} Legacy" // "[{hostname} #{servernum}]"

echo [L4L] Executed: l4l/legacy.cfg
echo

exec server