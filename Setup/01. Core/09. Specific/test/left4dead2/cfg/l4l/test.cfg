echo
echo [L4L] Executing: l4l/test.cfg

hostname "L4L Test" // ""
sn_hostname_format1 "{hostname} Test {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} Test {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} Test" // "[{hostname} #{servernum}]"
l4l_exec_server_config test // ""

echo [L4L] Executed: l4l/test.cfg
echo

exec server