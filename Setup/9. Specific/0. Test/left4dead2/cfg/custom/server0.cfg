echo
echo [L4L] Executing: custom/server0.cfg

hostname "L4L #0 Test" // ""
sn_hostname_format1 "{hostname} #{servernum} Test {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} #{servernum} Test {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} #{servernum} Test" // "[{hostname} #{servernum}]"

echo [L4L] Executed: custom/server0.cfg
echo

exec server