echo
echo [L4L] Executing: server6.cfg

hostname "L4L #6 Legacy" // ""
sn_host_num 6 // 0
sn_hostname_format1 "{hostname} #{servernum} Legacy {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} #{servernum} Legacy {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} #{servernum} Legacy" // "[{hostname} #{servernum}]"

echo [L4L] Executed: server6.cfg
echo

exec server