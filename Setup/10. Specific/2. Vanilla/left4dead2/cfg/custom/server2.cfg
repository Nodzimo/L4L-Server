echo
echo [L4L] Executing: custom/server2.cfg

hostname "L4L #2 Vanilla" // ""
sn_host_num 2 // 0
sn_hostname_format1 "{hostname} #{servernum} Vanilla {gamemode}" // "[{hostname} #{servernum}] {gamemode}"
sn_hostname_format2 "{hostname} #{servernum} Vanilla {gamemode} {difficulty}" // "[{hostname} #{servernum}] {gamemode} - {difficulty}"
sn_hostname_format3 "{hostname} #{servernum} Vanilla" // "[{hostname} #{servernum}]"

echo [L4L] Executed: custom/server2.cfg
echo

exec server